<div class="banner">
	<p> Libreo mission statement</p>
</div>

<div class="container">
    <div class="row">
        <div class="col-xs-12 text-center" id="top-bar">
            
        </div>
   </div>

    <div class = "row">
        <div class="col-sm-2">
            <div class="collection" id="">

                {{#each subject}}

                    <p>name: {{this.field_name}} </p>   
                {{/each}}
                <h3>Topics</h3>
                {{#each subject}}
                    <p> topic: {{this.subject_name}}
                {{/each}}

                {{tech.dataValues.subject_name}}
                {{biz}}
            </div>
            
        </div>

        {{#each subject}}
            <a href="/subject" class="collection-item">{{this.subject_name}}</a>
        {{/each}}
    </div>
</div>



